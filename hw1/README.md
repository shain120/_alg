分割 (Divide)： 將陣列從中間切一半，切成兩個子陣列。持續重複這個動作，直到每個子陣列只剩下 1 個元素 為止（因為只有 1 個元素的陣列必定是已排序的）。

擊破 (Conquer)： 這是遞迴 (Recursive) 的過程，實際上是在分割到底部後開始處理。

合併 (Combine/Merge)： 將兩個「已經排序好」的子陣列，透過比對大小，依序合併成一個更大的已排序陣列，直到還原成完整的陣列。
![img](https://github.com/shain120/_alg/blob/main/hw1/20250917.jpg)
